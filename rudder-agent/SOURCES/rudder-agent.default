# Rudder Agent init configuration
# Uncomment lines to change default values

# cf-execd(id 0): 0 to stop, 1 to start (default 1)
#CFENGINE_COMMUNITY_RUN_0="1"
#CFENGINE_COMMUNITY_PARAMS_0=""

# cf-serverd(id 1): 0 to stop, 1 to start (default 1)
#CFENGINE_COMMUNITY_RUN_1="1"
#CFENGINE_COMMUNITY_PARAMS_1="--info"

# Timeout before retrying a stop or a start (default 60s)
#TIMEOUT="60"

# cfengine syslog facility (default local6)
#SYSLOG_FACILITY="local6"

# Command used to find if a daemon is still running (default depend on the system)
#PS_COMMAND="ps -efww"
